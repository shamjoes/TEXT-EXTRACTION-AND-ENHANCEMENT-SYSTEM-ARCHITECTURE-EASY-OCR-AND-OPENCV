# TEXT-EXTRACTION-AND-ENHANCEMENT-SYSTEM-ARCHITECTURE-EASY-OCR-AND-OPENCV
The system takes an image or scanned document as input, enhances the quality of the text area using OpenCV, and then uses EasyOCR to automatically detect and extract the text. The extracted text is cleaned and given to the user in a readable and editable format (such as plain text or a text file).
